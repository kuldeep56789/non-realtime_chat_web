use tele;

/*create TABLE teleuser(
    email VARCHAR(255) PRIMARY KEY,
    username VARCHAR(255),
    password VARCHAR(255)
);*/


SELECT * FROM teleuser;

-- CREATE Table messages(
--     sender VARCHAR(255),
--     email VARCHAR(255),
--     message VARCHAR(255),
--     Foreign Key (email) REFERENCES teleuser(email),
--     id int auto_increment PRIMARY KEY
-- );

    -- drop Table messages

    SELECT * FROM messages;